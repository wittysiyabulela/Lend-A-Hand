<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:label="Lend A Hand"
        android:supportsRtl="true"
        android:theme="@style/Theme.LendAHand">

        <activity android:name=".DonationActivity" />
        <activity android:name=".DonationHistoryActivity" />
        <activity android:name=".Leaderboard" />
        <activity android:name=".RequestHelpActivity" />
        <activity android:name=".RequestHistoryActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".ThankYouActivity" />
        <activity android:name=".UserProfileActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".requestPage" />

        <activity
            android:name=".splashActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

    </application>

</manifest>